# module: lambda

Portal_API Lambda（Python, 256〜512MB, timeout=10s）、lambda-portal-role（DynamoDB 読取＋page_view_logs 書込、Product_A 書込なし）、CloudWatch Logs を定義する。

- 対応要件: Req 9.3, 14.3, 18.3
- 実装は Task 14.3 で追加する（プレースホルダ）。
